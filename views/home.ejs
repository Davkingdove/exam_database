<!DOCTYPE html>
<html lang="en">
<head>
    <style>
      /*styling table  and inpugt forms */
        table, td, th {  
          border: 1px solid #ddd;
          text-align: left;
        }
        
        table {
          border-collapse: collapse;
          width: 35%;
        }
        
        th, td {
          padding: 10px;
        }
        input[type='number']{
          width: 80px;
        }
        input[type='submit']{
          background: #0c7715;
          margin: 15px;
          width: 90px;
        }
        /* Styles for toast modal */
  .modal {
    display: none;
    position: fixed;
    z-index: 9999;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    background-color: #f2f2f2;
    border: 1px solid #888;
    border-radius: 5px;
    padding: 20px;
  }
  
  .modal-content {
    text-align: center;
  }
  
  .close {
    float: right;
    cursor: pointer;
    font-size: 20px;
  }
        </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HOME</title>
</head>
<body>

    <form action="/save" method="POST">
        <label for="firstname">first Name</label>
        <input type="text" name="firstname" id="firstname">
        <label for="firstname">Last Name</label>
        <input type="text" name="lastname" id="lastname">
        <table>
            <tr>
              <th>SUBJECT</th>
              <th>30%</th>
              <th>70%</th>
            </tr>
            <tr>
              <td>ENGLISH</td>
              <td><input type="number" name="english30" required></td>
              <td><input type="number" name="english70" required></td>
            </tr>
            <tr>
              <td>CORE MATHS</td>
              <td><input type="number" name="cmaths30" required></td>
              <td><input type="number" name="cmaths70" required></td>
            </tr>
            <tr>
              <td>SOCIAL STUDIES</td>
              <td><input type="number" name="studies30" required></td>
              <td><input type="number" name="studies70" required></td>
            </tr>
            <tr>
              <td>INTEGRATED SCIENCE</td>
              <td><input type="number" name="science30" required></td>
              <td><input type="number" name="science70" required></td>
            </tr>
            <tr>
                <td>CHEMISTRY</td>
                <td><input type="number" name="chemistry30" required></td>
                <td><input type="number" name="chemistry70" required></td>
              </tr>
              <tr>
                <td>MATHEMATICS</td>
                <td><input type="number" name="maths30" required></td>
                <td><input type="number" name="maths70" required></td>
              </tr>
              <tr>
                <td>PHYSICS</td>
                <td><input type="number" name="physics30" required></td>
                <td><input type="number" name="physics70" required></td>
              </tr>
              <tr>
                <td>BIOLOGY</td>
                <td><input type="number" name="biology30" required></td>
                <td><input type="number" name="biology70" required></td>
              </tr>
        </table> 
<input type="submit" value="save">
    </form>

    <!-- Modal for toast messages -->
<div id="toastModal" class="modal">
  <div class="modal-content">
    <span class="close">&times;</span>
    <p id="toastMessage"></p>
  </div>
</div>
<script>
  document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('dataForm');
    const toastModal = document.getElementById('toastModal');
    const toastMessage = document.getElementById('toastMessage');
    const closeBtn = document.querySelector('.close');
  
    form.addEventListener('submit', async (event) => {
      event.preventDefault();
  
      const formData = new FormData(form);
      const firstname = formData.get('firstname');
      const lastname = formData.get('lastname');
  
      const response = await fetch('/save', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json'
        },
        body: JSON.stringify({ firstname, lastname })
      });
  
      const data = await response.json();
  
      if (response.ok) {
        showToast(data.message);
      } else {
        showToastError(data.message);
      }
    });
    // Function to display success or info toast
    function showToast(message) {
      toastModal.style.display = 'block';
      toastMessage.textContent = message;
  
      closeBtn.onclick = function() {
        toastModal.style.display = 'none';
      }
      setTimeout(() => {
        toastModal.style.display = 'none';
      }, 3000);
    }
    // Function to display error toast
    function showToastError(message) {
      toastModal.style.backgroundColor = '#ffcccc'; // Change background color for error
      showToast(message);
    }
  });
</script>
</body>
</html>